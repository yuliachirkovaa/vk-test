import { FC } from "react";

const Header: FC = () => {

  return (

    <div>

      <a href = "/">Главная</a>
      <a href = "/favorites">Избранное</a>

    </div>

  );

};

export default Header;